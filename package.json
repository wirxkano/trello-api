{
  "name": "trello-api",
  "type": "module",
  "version": "1.0.0",
  "description": "api",
  "main": "src/server.js",
  "scripts": {
    "start": "node --watch src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "server"
  ],
  "author": "wir",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
